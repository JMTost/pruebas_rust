[package]
name = "api_prueba"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
diesel = {version = "1.4.4", features = ["postgres", "r2d2", "chrono"]}
dotenv = "0.15.0"